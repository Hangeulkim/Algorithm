#include<iostream>
#include<string>
#include<stack>

using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int testcase;
    cin>>testcase;
    string input,ans="";
    while(testcase--){
        cin>>input;
        for(int i=0;i<input.length();i++){
            switch(input[i]){

            }
        }
    }

    return 0;
}
